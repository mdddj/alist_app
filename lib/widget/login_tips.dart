part of 'part.dart';

///登录提示
class LoginTip extends StatelessWidget {
  const LoginTip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("请先登录"),);
  }
}
